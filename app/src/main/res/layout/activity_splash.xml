<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/splashRoot"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/black"
    android:clipToPadding="false"
    android:clipChildren="false">

    <!-- Animated TileVision grid background -->
    <de.westnordost.streetmeasure.GridBackgroundView
        android:id="@+id/gridBackground"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:alpha="0.9"/>

    <!-- Centered column for logo + text -->
    <LinearLayout
        android:id="@+id/brandingColumn"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center_horizontal"
        android:layout_gravity="center"
        android:clipToPadding="false"
        android:clipChildren="false"
        android:paddingStart="24dp"
        android:paddingEnd="24dp">

        <!-- Logo container with ripple underneath -->
        <FrameLayout
            android:id="@+id/logoContainer"
            android:layout_width="200dp"
            android:layout_height="200dp"
            android:layout_gravity="center_horizontal"
            android:clipToPadding="false"
            android:clipChildren="false">

            <!-- Expanding teal pulse ring -->
            <View
                android:id="@+id/ripplePulse"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:background="@drawable/bg_ripple_circle"
                android:alpha="0"
                android:scaleX="0.2"
                android:scaleY="0.2"/>

            <!-- The app icon / logo -->
            <ImageView
                android:id="@+id/logoImage"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:adjustViewBounds="true"
                android:scaleType="fitCenter"
                android:src="@mipmap/ic_launcher"
                android:contentDescription="@string/app_name"
                android:alpha="0"
                android:scaleX="0.85"
                android:scaleY="0.85"/>
        </FrameLayout>

        <!-- App title -->
        <TextView
            android:id="@+id/appTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="TileVision AR"
            android:textSize="28sp"
            android:textStyle="bold"
            android:fontFamily="sans-serif-medium"
            android:layout_marginTop="16dp"
            android:textColor="@color/splashTitleColor"
            android:alpha="0"/>

        <!-- Tagline -->
        <TextView
            android:id="@+id/tagline"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Measure. Visualize. Estimate."
            android:textSize="16sp"
            android:fontFamily="sans-serif"
            android:layout_marginTop="8dp"
            android:textColor="@color/splashSubtitleColor"
            android:alpha="0"/>
    </LinearLayout>
</FrameLayout>
